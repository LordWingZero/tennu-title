{
  "name": "tennu-title",
  "version": "1.0.3",
  "description": "A plugin for the tennu IRC framework, uses logging db to find last sent URL and retirves the title for it.",
  "main": "plugin.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/LordWingZero/tennu-aseen"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push && git push --tags && npm publish"
  },
  "author": "Victorio Berra",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.10.6",
    "node-fetch": "^1.3.3"
  }
}
